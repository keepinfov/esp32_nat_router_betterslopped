# ESP32 target overrides

# 240 MHz is a per-chip choice, so it is set per target rather than in the
# shared defaults: the C3 and C6 have no such symbol and warn about it.
CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_240=y
